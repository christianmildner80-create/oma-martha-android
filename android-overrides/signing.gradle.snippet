    signingConfigs {
        debug {
            storeFile file("oma-martha-debug.keystore")
            storePassword "android"
            keyAlias "androiddebugkey"
            keyPassword "android"
        }
    }
